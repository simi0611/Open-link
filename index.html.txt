

<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Quick Question</title>
  <style>
    :root {
      --bg: #0b0b10;
      --card: rgba(255,255,255,0.08);
      --text: #ffffff;
      --muted: rgba(255,255,255,0.75);
      --btn: rgba(255,255,255,0.12);
      --btnHover: rgba(255,255,255,0.18);
      --border: rgba(255,255,255,0.18);
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      min-height: 100vh;
      display: grid;
      place-items: center;
      padding: 18px;
      background: radial-gradient(1200px 800px at 50% 20%, #1b1b3a 0%, var(--bg) 60%);
      color: var(--text);
      font-family: "Times New Roman", Times, serif; /* Times in roman */
    }

    .wrap {
      width: 100%;
      max-width: 520px;
    }

    .card {
      border: 1px solid var(--border);
      background: var(--card);
      backdrop-filter: blur(10px);
      border-radius: 18px;
      padding: 22px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.45);
    }

    h1 {
      margin: 0 0 10px;
      font-size: 28px;
      line-height: 1.15;
      letter-spacing: 0.2px;
    }

    p {
      margin: 0 0 18px;
      color: var(--muted);
      font-size: 18px;
      line-height: 1.4;
    }

    .buttons {
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
      margin-top: 10px;
    }

    button {
      flex: 1;
      min-width: 140px;
      padding: 14px 16px;
      border-radius: 14px;
      border: 1px solid var(--border);
      background: var(--btn);
      color: var(--text);
      font-family: "Times New Roman", Times, serif;
      font-size: 18px;
      cursor: pointer;
      transition: transform 0.05s ease, background 0.15s ease;
      user-select: none;
      -webkit-tap-highlight-color: transparent;
    }

    button:active { transform: scale(0.98); }
    button:hover { background: var(--btnHover); }

    .msg {
      margin-top: 16px;
      display: none;
      padding: 14px 14px;
      border-radius: 14px;
      border: 1px solid var(--border);
      background: rgba(0,0,0,0.25);
      font-size: 18px;
      line-height: 1.35;
    }

    .small {
      margin-top: 10px;
      font-size: 14px;
      color: rgba(255,255,255,0.55);
    }

    .hidden { display: none !important; }

    /* make it feel nice on phone */
    @media (max-width: 380px) {
      h1 { font-size: 24px; }
      p, button, .msg { font-size: 17px; }
    }
  </style>
</head>
<body>
  <div class="wrap">
    <div class="card">
      <h1>r u gay?</h1>
      <p>Pick an option 👇</p>

      <div class="buttons">
        <button id="yesBtn" type="button">Yes</button>
        <button id="noBtn" type="button">No</button>
      </div>

      <div id="msgBox" class="msg" aria-live="polite"></div>
      <div id="countLine" class="small"></div>
    </div>
  </div>

  <script>
    const yesBtn = document.getElementById("yesBtn");
    const noBtn = document.getElementById("noBtn");
    const msgBox = document.getElementById("msgBox");
    const countLine = document.getElementById("countLine");

    let noCount = 0;
    const maxNo = 5;

    // You can change these lines if you want different sass
    const sassyMessages = [
      "Bestie… don’t lie 😌",
      "No? That’s what they all say 😏",
      "Okayyyy sure 😭",
      "Your phone says otherwise 💅",
      "Last chance… be honest 😌"
    ];

    function showMessage(text) {
      msgBox.textContent = text;
      msgBox.style.display = "block";
    }

    yesBtn.addEventListener("click", () => {
      showMessage("im proud of being gay happy pride 🏳️‍🌈✨");
      countLine.textContent = "";
    });

    noBtn.addEventListener("click", () => {
      if (noCount < maxNo) {
        showMessage(sassyMessages[noCount] || "Okay 😌");
        noCount++;

        // optional counter text
        const left = maxNo - noCount;
        countLine.textContent = left > 0
          ? `No presses left: ${left}`
          : "No button is gone 😌";

        if (noCount >= maxNo) {
          // hide No after 5 presses
          noBtn.classList.add("hidden");
        }
      }
    });
  </script>
</body>
</html>